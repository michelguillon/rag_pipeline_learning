# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# Secrets / environment
.env
.env.*
*.key
# ...but the template IS committed (shows required vars, no real key)
!.env.example

# Vector store — large, machine-specific, regenerated by ingest.py
chroma_db/
*.sqlite3

# Real CV — never committed. Keep the data/ folder itself via .gitkeep.
data/*.docx
data/*.pdf
data/*.txt
!data/.gitkeep

# outputs/ is tracked intentionally (experiment results = interview artifact).
# Before going public: regenerate outputs with the fake sample CV.

# Claude Code local settings (machine-specific)
.claude/settings.local.json

# OS / editor noise
.DS_Store
Thumbs.db
.vscode/
.idea/
