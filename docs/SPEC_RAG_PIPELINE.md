# RAG Pipeline — Project Spec
**AI Learning Track | Partner Solution Architect Preparation**

This document is the single source of truth for the RAG pipeline build. It
captures every architectural decision, the reasoning behind it, and the
ordered implementation steps for Claude Code.

The spec is split into two parts:

- **Part 1 — Phase 1 (Week 1).** The original single-CV pipeline.
- **Part 2 — Phase 2.** Config-driven chunker, common Paragraph model, PDF loader.

Both phases are complete and merged to `main`. New work appends new parts;
older parts stay as the architectural record.

Work in two modes:
- **Here (this doc + conversation)** — architecture, decisions, reasoning
- **Claude Code** — implementation only, guided by the steps below

---

## What we're building

A document Q&A system that ingests a CV, stores it in ChromaDB, retrieves
relevant chunks on query, and passes them to Mistral for answer generation.

The pipeline runs as separate scripts with clean separation of concerns:

```
analyse.py        → inspect document structure, call Mistral for chunking recommendation
review_chunks.py  → preview proposed chunks, human approves before any embedding
ingest.py         → chunk + embed + store in ChromaDB (run once per document)
query.py          → embed query + retrieve + generate answer (run any time)
```

The full data flow:

```
Document
   └─► analyse.py ──► Mistral API ──► Recommended config (printed)
                                              │
                                       Human approves
                                              │
                                       config.json
                                              │
   └─► review_chunks.py ──► Preview chunks (printed, no embedding yet)
                                              │
                                       Human confirms
                                              │
   └─► ingest.py ──► Chunker ──► mistral-embed ──► ChromaDB (persistent)
                                              │
   └─► query.py ──► embed query ──► ChromaDB ──► Mistral ──► Answer
```

---

## Environment

**Development:** Windows, Docker Desktop, PowerShell
**Target deployment:** Home server (arriving Wednesday) — same Docker image, zero changes needed

### Why Docker (not venv)

venv and Docker solve the same problem — isolated, reproducible dependencies — at different levels. venv is Python-level isolation on your local machine. Docker is system-level isolation: a complete Linux environment with its own Python, packages, and filesystem. Nothing touches Windows except the files explicitly mounted as volumes.

Choosing Docker from the start means:
- No retrofitting later when moving to the home server
- ChromaDB data persists in a named volume across container restarts
- Anyone cloning the repo gets a working environment with one command
- Closer to how this would run in a real enterprise deployment

**Architecture: one container, not two.** ChromaDB runs as an embedded Python library (not a server), so there's no need for a separate ChromaDB service in Docker Compose. One `pipeline` service, one volume mount for the data directory.

```yaml
# docker-compose.yml
services:
  pipeline:
    build: .
    volumes:
      - .:/app                       # whole project: live source + persistent
                                     # state (config.json, chroma_db/, outputs/)
    environment:
      - MISTRAL_API_KEY=${MISTRAL_API_KEY}
    stdin_open: true
    tty: true                        # needed for interactive approval prompts
```

```dockerfile
# Dockerfile
FROM python:3.13-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
```

**Running scripts inside the container:**
```powershell
# Run a specific script
docker compose run pipeline python analyse.py data/cv.docx

# Interactive shell for exploration
docker compose run pipeline bash
```

**API key:** stored in a `.env` file at project root, never committed.
```powershell
# .env (git-ignored)
MISTRAL_API_KEY=your-key-here
```
Docker Compose automatically reads `.env` — no export needed.

### SDK note (v2.x)
```python
from mistralai.client import Mistral   # v2.x — NOT: from mistralai import Mistral
```
`requirements.txt` pins `mistralai>=2.0.0,<3.0.0` — v2.x is a breaking rewrite of v1.x.

---

## Git repository

**Visibility:** Private during development. Public when ready to release.

**Public release checklist (do before making public):**
- [ ] Replace real CV with `data/sample_cv.docx` (fake document, same structure)
- [ ] Audit `outputs/` for any chunks containing real CV text — remove or regenerate with fake CV
- [ ] Confirm `.env` is in `.gitignore` and not in history
- [ ] Write README (see README structure section at end of this doc)

### .gitignore
```
# Environment & secrets
.env

# Vector store — large, machine-specific, regenerated by ingest.py
chroma_db/

# Real CV — never committed
data/*.docx
data/*.pdf
data/*.txt

# Keep the data folder itself
!data/.gitkeep

# Outputs — included in repo (interview artifact value)
# Exception: regenerate with fake CV before going public
# outputs/ is tracked intentionally

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
```

### Repository structure
```
rag-pipeline-week1/          ← public repo name suggestion
├── .env.example             ← shows required variables, safe to commit
├── .gitignore
├── Dockerfile
├── docker-compose.yml
├── requirements.txt
├── README.md                ← written last, after stress tests
├── SPEC.md                  ← this document
├── data/
│   ├── .gitkeep
│   └── sample_cv.docx       ← added at public release (fake CV)
├── outputs/                 ← experiment results, stress test JSON
│   └── .gitkeep
├── chroma_db/               ← git-ignored, created by ingest.py
├── analyse.py
├── review_chunks.py         ← (spec Step 2; renamed from inspect.py — see note below)
├── ingest.py
├── query.py
├── stress_test.py
└── config.json              ← written by analyse.py, safe to commit
```

**.env.example:**
```
# Copy to .env and fill in your key
# Never commit .env
MISTRAL_API_KEY=your-mistral-api-key-here
```

---

## Architecture decisions

### Decision 1 — Document parsing

**First read (superseded — kept for the reasoning trail):**
> The CV is a single 5-column table (26 rows). Read column 0 only — the other
> 4 columns are layout duplicates. Detect structure by paragraph style:
> `Heading 3` → company or job title; `List Paragraph` → bullet.

**What the actual document showed (Phase 2 probe, verified against raw XML):**

The first read assumed more style discipline than the document has — a common
real-world trap. The probe found:

- **It is a one-column table, not five.** Every row is a single cell with
  `gridSpan=5`; `python-docx` returns that one cell five times. Reading
  `cells[0]` works — because there is only one cell per row, not because we
  discard four duplicates.
- **Zero document-level paragraphs** with content — confirmed; the CV is
  entirely table-based.
- **Hierarchy is encoded inconsistently.** The author mixed Word heading
  *styles* with direct font-size formatting. Paragraph style alone is not a
  reliable signal — the decode needs three signals together:

| Role | Reliable signal | Renders at |
|------|-----------------|-----------|
| Section header | `Heading 1` style | 16 pt |
| Company / sub-section header | direct **14 pt** size override (style varies: Normal / Heading 3 / Heading 4) | 14 pt |
| Job title | `Heading 3` style, no size override | 11 pt |
| Bullet (achievement) | has a `numPr` element | 11 pt |
| Body text | `Normal`, no `numPr`, not 14 pt | 11–12 pt |

- **The trap:** "Microsoft" uses `Heading 3` — the same style as job titles.
  Style alone cannot separate them; the 14 pt override does.
- **Bullets:** detect by `numPr`, NOT by the `List Paragraph` style — bullets
  appear under both `List Paragraph` and `Normal` (numId 6/7/9).

**Decode rule (ordered):** `numPr` → bullet; rendered size 14 pt → company /
sub-section; `Heading 3` → job title; `Heading 1` → section; else → body.

**Parsing approach:**
- `python-docx`, walk `doc.tables[0]`, read `cells[0]` of each row.
- Do NOT hardcode the size/style thresholds. `analyse.py` *profiles* the
  document (see Decision 3) and the decode mapping is confirmed per-document —
  so the pipeline generalises to other document types, not just this CV.

---

### Decision 2 — Chunking strategy

We build **two collections** to run as a controlled experiment.

#### Option A — One chunk per role

Each chunk = one complete job entry (all bullets for that role combined).

```
Chunk example:
  company:  Utiq
  title:    Head of Solution Consulting
  dates:    Mar 2024 – present
  text:     "Joined at a pre-commercial stage to define and establish
             the UK solutions consulting function...
             Worked directly with C-level clients...
             Activated initial pilot campaigns..."
```

**Why this works:** Role-level chunks preserve full context for each
position. A query like "what did he do at Utiq?" retrieves the entire
entry rather than a fragment.

**Risk:** A query about a specific achievement competes with everything
else in that role's chunk. The embedding has to average across all bullets,
which dilutes the signal for precise queries.

#### Option A.2 — One chunk per bullet, with context prefix

Each bullet becomes its own chunk, but carries a mandatory prefix so it
remains self-contained.

```
Chunk example:
  prefix:  [Utiq | Head of Solution Consulting | Mar 2024–present]
  text:    "Activated initial pilot campaigns across multiple publisher
            partners, achieving 40% incremental audience reach and
            informing the broader commercialisation roadmap."
```

**Why the prefix is mandatory:** Bullets in isolation have no anchor to
the company or role. Without the prefix, a query like "what did he achieve
at Utiq?" would not retrieve a bullet that never mentions "Utiq" by name.
The prefix is not decoration — it is load-bearing for retrieval.

**Risk:** Bullets are semantically narrow. A question requiring synthesis
across a role (e.g. "summarise his responsibilities at Microsoft") will
retrieve fragments, not the full picture.

**Why we test both:** The experiment reveals whether retrieval precision
(A.2) or retrieval context (A) produces better answers for this type of
document. Neither is obviously correct — the right answer depends on the
query distribution, which we'll discover empirically.

**Expected chunk counts (approximate):**
- Option A: ~7 chunks (one per role + education + other sections)
- Option A.2: ~20–25 chunks (one per bullet across all roles)

---

### Decision 3 — Document analyser with Mistral recommendation

Before chunking, `analyse.py` runs a structural analysis of the document
and calls Mistral to recommend a chunking strategy.

**Why this exists:**
- Forces explicit reasoning about document structure before coding it
- Makes the pipeline reusable for other document types
- Demonstrates a meta-pattern in interviews: AI reasoning about AI pipeline
  configuration

**Human-in-the-loop by design:**
The analyser prints a recommended config but does not apply it
automatically. The human reviews and confirms before ingestion begins.
This is a legitimate production pattern for enterprise RAG where document
types vary and automatic misconfiguration is costly.

**What the analyser detects:**
- Document structure type (paragraph-based, table-based, mixed)
- A **formatting-fingerprint profile**: every distinct combination of
  (paragraph style, rendered font size, bold, list-or-not) with occurrence
  counts and a sample line — the document-agnostic core of the analyser
- A **consistency report**: objective flags where visual hierarchy is NOT
  driven by styles (bullets under multiple styles; prominent text carrying no
  heading style; heading styles overridden by direct sizing)
- Detected sections (via heading styles) with text and bullet counts
- Estimated chunk counts for Option A and Option A.2
- Average content length per section

Why fingerprints, not hardcoded rules: profiling rather than assuming makes the
analyser reusable across document types, not just this CV.

**What it asks Mistral:**
Given the structural analysis above, Mistral recommends:
- Which chunking strategy fits this document type
- Where the natural chunk boundaries are
- What metadata to attach to each chunk
- Any risks or edge cases to watch for

**Output:** A `config.json` file that `review_chunks.py` and `ingest.py` read.

---

### Decision 4 — Vector store

**Choice: ChromaDB in persistent mode**

```python
import chromadb
client = chromadb.PersistentClient(path="./chroma_db")
```

**Why persistent (not ephemeral):**
We have multiple collections that must coexist. Ephemeral mode loses all
collections on every restart — unacceptable when running experiments across
Option A, Option A.2, and multiple distance metrics.

**Why ChromaDB (not Pinecone, Weaviate, pgvector):**
- Zero setup, runs locally
- Python-native, no external service to manage
- Persistent client writes to `./chroma_db` directory — portable via Docker
- Sufficient for this corpus size (~25 chunks max)

**Production upgrade path:**
ChromaDB → Pinecone when: corpus exceeds ~1M vectors, multi-tenant access
required, or managed infrastructure is preferred. The abstraction layer
in `ingest.py` means this swap is a one-file change.

**Docker:** ChromaDB data directory mounted as a Docker volume so it
survives container restarts and can be backed up.

**Collection naming convention (baked in from creation — cannot change metric after):**
```
cv_role_cosine     ← Option A, cosine distance
cv_role_l2         ← Option A, L2 distance
cv_bullet_cosine   ← Option A.2, cosine distance
cv_bullet_l2       ← Option A.2, L2 distance
```

---

### Decision 5 — Distance metric

We test two metrics. All three options are documented here so the
reasoning for exclusions is explicit.

#### Cosine similarity ✅ — tested
Measures the **angle** between two vectors regardless of their magnitude.
Two documents expressing the same idea with different amounts of text
will still land close in vector space because cosine is magnitude-independent.
Standard choice for text similarity.

Formula: `cos(θ) = (A · B) / (|A| × |B|)`
Range: -1 (opposite) to 1 (identical). ChromaDB reports as distance (1 - similarity).

#### L2 (Euclidean) distance ✅ — tested
Measures the **straight-line distance** between two points in vector space.
Sensitive to magnitude. On normalised vectors (where all magnitudes = 1)
the divergence from cosine is smaller but not zero — closely ranked chunks
can flip order, which is what makes this worth testing.

Formula: `L2 = √(Σ(Aᵢ - Bᵢ)²)`

**Why L2 can produce different rankings than cosine on normalised vectors:**
Even with magnitude = 1, the geometric relationship between distance and
angle is non-linear. Pairs of vectors that cosine ranks similarly may have
a different L2 ordering, particularly when similarity scores are close.
The experiment is designed to surface whether these ranking differences
affect answer quality in practice.

#### Inner product (dot product) ❌ — excluded
When vectors are L2-normalised (magnitude = 1), inner product equals
cosine similarity exactly: `A · B = |A||B|cos(θ) = cos(θ)` when |A|=|B|=1.

`mistral-embed` produces L2-normalised vectors (confirmed in learning
notes from Experiment 6). Testing inner product would confirm the maths
but produce identical retrieval rankings to cosine — no new information.
Excluded on those grounds, not because it's inferior.

**ChromaDB metric setting (set at collection creation, immutable):**
```python
collection = client.create_collection(
    name="cv_role_cosine",
    metadata={"hnsw:space": "cosine"}  # or "l2"
)
```

---

### Decision 6 — top_k

top_k is calibrated to collection size, not set as a global constant.
The guiding heuristic: top_k should not exceed ~40% of collection size,
beyond which retrieval collapses into near-full-collection return.

**The three-way tradeoff top_k sits inside:**
```
Retrieval coverage  ←──── top_k ────►  Token cost + Noise
```
More chunks = better coverage of multi-part answers, higher prompt token
cost, higher risk of irrelevant content confusing the model. In production,
token budget is usually the binding constraint, not retrieval quality alone.

#### Option A (~7 chunks):
- **Min: top_k = 1** — forces the system to commit to a single best match.
  Reveals whether the top result alone is sufficient and exposes cases where
  the right answer is in the second or third chunk.
- **Max: top_k = 3** — 3/7 = 43%, approximately the 40% ceiling.
  Sufficient to cover multi-role queries without returning the whole collection.

#### Option A.2 (~22 chunks):
- **Min: top_k = 1** — same reasoning as above.
- **Max: top_k = 5** — 5/22 = 23%, more conservative than the 40% ceiling.
  Bullet chunks are semantically narrow, so more chunks are needed to
  reconstruct a complete answer — but token budget caps the ceiling.
  Note: this is where token budget overrides the pure retrieval heuristic.

**Why we skip the middle value:**
The min/max comparison reveals the failure modes (too little context vs.
too much noise). The middle value is where you'd land in production — but
for learning, the boundaries teach more than the compromise.

---

### Decision 7 — Embedding model

**Choice: `mistral-embed` (fixed)**

Key properties (from Experiment 6 in learning notes):
- Output: 1024-dimensional vectors
- Vectors are **L2-normalised** (magnitude ≈ 1.0)
- Consequence: cosine similarity = dot product for these vectors
- Batch input: list of strings, output: one vector per string in order

**The one sticky choice in the pipeline:**
The embedding model is the only component that cannot be swapped freely.
Changing it means re-embedding (re-indexing) the entire corpus because
vectors from different models live in incompatible vector spaces.
The generation model, prompt, and top_k can all change without re-indexing.

**Same model for documents and queries — always:**
Retrieval works by comparing query vector to document vectors. If they
were embedded by different models, the comparison is meaningless — the
vectors exist in different geometric spaces.

---

### Decision 8 — Generation model

We test two models against the same retrieved chunks.

**Default: `mistral-small-latest`**
Fast, cheap, good for extraction and summarisation. CV Q&A is not a hard
reasoning task — the model needs to read context and report, not reason
across multiple inference steps.

**Comparison: `mistral-large-latest`**
Best reasoning quality, ~10x the cost of small. The experiment question:
does the quality improvement justify the cost for simple document Q&A?
Expected finding: marginal difference for straightforward CV queries,
more meaningful difference for synthesis or multi-hop questions.

**Temperature: 0.1 (fixed)**
Near-deterministic. Factual Q&A requires consistency, not creativity.
High temperature increases hallucination risk when the model should be
anchored to retrieved context. (Established in Experiment 3.)

---

### Decision 9 — Prompt design

#### Instruction (fixed — not an experiment variable)

```
You are a precise assistant answering questions about a CV.
Answer using ONLY the information in the context provided below.
If the answer is not present in the context, respond with exactly:
"I cannot find this in the provided document."
Do not use your general knowledge. Do not infer or extrapolate.
```

**Why the exact fallback phrase matters:**
It gives us a testable hallucination signal. Any response that isn't
that exact phrase for an out-of-scope question means the model drew on
training data. This makes the stress test (Hours 8–10) measurable.

#### Context format (experiment variable)

Two formats tested with the same retrieved chunks:

**Flat:**
```
Context:
[chunk text]
[chunk text]
[chunk text]

Question: ...
```

**Labelled:**
```
Context:
[Source: Utiq | Head of Solution Consulting | Mar 2024–present]
[chunk text]

[Source: Microsoft | Senior Product Manager | Apr 2022–Mar 2024]
[chunk text]

Question: ...
```

**Why labelled may outperform flat:**
The model can attribute answers to specific roles, reducing confusion when
similar content appears across multiple positions. Also makes hallucination
easier to detect — a wrong source citation is a visible error.

---

## Experiment matrix

Every experiment uses the same set of test questions (defined in `query.py`).

| Axis | Values |
|------|--------|
| Chunk strategy | A (by role) / A.2 (by bullet + prefix) |
| Distance metric | cosine / L2 |
| top_k | min / max (calibrated to collection) |
| Generation model | mistral-small / mistral-large |
| Context format | flat / labelled |

**Collections:** 4 (chunk strategy × distance metric)
**Query combinations per collection:** 4 (top_k × model)
**Context format:** tested at query time, not collection time

---

## Implementation steps for Claude Code

Work through these in order. Do not move to the next step until the
current one produces correct output.

---

### Step 0 — Project scaffold

```
rag_pipeline/
├── docker-compose.yml
├── requirements.txt
├── .env.example
├── chroma_db/          ← ChromaDB persistent storage (git-ignored)
├── data/               ← Drop CV here
│   └── .gitkeep
├── outputs/            ← Experiment results, JSON logs
│   └── .gitkeep
├── analyse.py
├── review_chunks.py    ← renamed from inspect.py (stdlib collision — see Step 2)
├── ingest.py
├── query.py
└── config.json         ← Written by analyse.py, read by review_chunks/ingest
```

**Docker Compose spec:**
```yaml
services:
  pipeline:
    build: .
    volumes:
      - .:/app   # whole project mounted: config.json and other state persist,
                 # and code edits go live without a rebuild
    environment:
      - MISTRAL_API_KEY=${MISTRAL_API_KEY}
```

**requirements.txt:**
```
mistralai>=2.0.0,<3.0.0
chromadb>=0.5.0
python-docx>=1.1.0
```

**Claude Code prompt:**
> Create the project scaffold defined in SPEC.md. Specifically:
> - Dockerfile using python:3.13-slim
> - docker-compose.yml with one pipeline service, volume mounts for
>   chroma_db/, data/, and outputs/, and MISTRAL_API_KEY from .env.
>   Include stdin_open and tty for interactive prompts.
> - requirements.txt pinned as specified
> - .gitignore as specified — data/*.docx excluded, outputs/ tracked,
>   chroma_db/ excluded
> - .env.example with MISTRAL_API_KEY placeholder
> - All directories with .gitkeep files
> - Empty placeholder files for analyse.py, review_chunks.py, ingest.py,
>   query.py, stress_test.py
> Do not write any pipeline logic yet. Verify the Docker setup builds
> cleanly with: docker compose build

---

### Step 1 — Document analyser (`analyse.py`)

**What it does:**
1. Accepts a document path as CLI argument
2. Parses the `.docx` using `python-docx`, column 0 only
3. Builds a structural summary:
   - Document type detected (table-based, paragraph-based)
   - All paragraph styles found and their frequency
   - List of detected sections with heading text and bullet count
   - Estimated chunk counts for Option A and Option A.2
   - Average word count per section
4. Calls Mistral API with the structural summary as context
5. Asks Mistral to recommend chunking strategy and metadata schema
6. Prints the recommendation clearly to terminal
7. Asks user: "Accept this config? (y/n)"
8. On y: writes `config.json`

**config.json schema:**
```json
{
  "strategy": "A",
  "document_type": "table_based",
  "boundary_signal": "Heading 3",
  "bullet_signal": "List Paragraph + is_list=True",
  "collections": {
    "A": ["cv_role_cosine", "cv_role_l2"],
    "A2": ["cv_bullet_cosine", "cv_bullet_l2"]
  },
  "metadata_fields": ["company", "title", "dates", "section_type"],
  "prefix_template": "[{company} | {title} | {dates}]"
}
```

**Mistral call context to include:**
- Structural summary dict (styles, section count, estimated chunks)
- The two strategy options (A and A.2) with their tradeoffs
- Ask for: recommended strategy, metadata schema, any document-specific risks

**Claude Code prompt:**
> Build analyse.py according to the spec in the project document.
> The Mistral call should pass the full structural analysis as context
> and ask for a chunking recommendation. Use the call_with_retry()
> pattern from the learning notes for the API call. Print the
> recommendation clearly, prompt for approval, write config.json on
> approval. CLI: `python analyse.py data/cv.docx`

---

### Step 2 — Chunk reviewer (`review_chunks.py`)

> **Naming note:** this step was originally specced as `inspect.py`. That name
> is unusable — `inspect` is a Python stdlib module, and a file by that name in
> the project root shadows it for *every* script (chromadb imports stdlib
> `inspect` on its first import line). Renamed to `review_chunks.py` in Phase 1.

**What it does:**
1. Reads `config.json`
2. Parses the document using the configured strategy
3. Prints every proposed chunk to terminal — no embedding, no storage
4. For each chunk shows: index, section type, metadata, full text, word count
5. Prints summary: total chunks, avg word count, min/max word count
6. Asks user: "Proceed to ingestion? (y/n)"

**Why this step exists:**
Embedding costs tokens and writes to ChromaDB. Inspecting first means
you catch bad chunk boundaries before spending API budget on them.
In production this is the equivalent of a data validation step before
a batch job.

**Example output format:**
```
─────────────────────────────────────────
CHUNK 003 | type=role | strategy=A
  company: Utiq
  title:   Head of Solution Consulting
  dates:   Mar 2024 – present
  words:   87
─────────────────────────────────────────
Joined at a pre-commercial stage to define and establish the UK
solutions consulting function, including operating model...
[full text]
─────────────────────────────────────────

Summary: 7 chunks | avg 72 words | min 31 | max 112
Proceed to ingestion? (y/n):
```

**Claude Code prompt:**
> Build review_chunks.py according to the spec. It reads config.json and
> the document path from CLI, prints all proposed chunks in the format
> shown, prints a summary, and prompts for confirmation.
> CLI: `python review_chunks.py data/cv.docx`

---

### Step 3 — Ingestion (`ingest.py`)

**What it does:**
1. Reads `config.json`
2. Chunks the document according to configured strategy
3. Creates 4 ChromaDB collections (or skips if already exist):
   - `cv_role_cosine`, `cv_role_l2`
   - `cv_bullet_cosine`, `cv_bullet_l2`
4. Embeds all chunks via `mistral-embed` (batched, with retry)
5. Stores chunks + embeddings + metadata in each collection
6. Prints ingestion summary per collection

**ChromaDB client:**
```python
import chromadb
client = chromadb.PersistentClient(path="./chroma_db")
```

**Collection creation:**
```python
# metric set at creation — immutable after
collection = client.get_or_create_collection(
    name="cv_role_cosine",
    metadata={"hnsw:space": "cosine"}
)
```

**Metadata schema per chunk:**
```python
{
    "company": "Utiq",
    "title": "Head of Solution Consulting",
    "dates": "Mar 2024 – present",
    "section_type": "role",        # or "education", "profile", etc.
    "strategy": "A",               # or "A2"
    "chunk_index": 2,
    "word_count": 87
}
```

**Embedding batching:**
- Batch size: 16 (conservative for free tier rate limits)
- Retry pattern: exponential backoff, retryable on 429 and 5xx only
- Sleep 0.5s between batches

**Claude Code prompt:**
> Build ingest.py according to the spec. It must create all 4 collections
> with the correct distance metrics, embed using mistral-embed in batches
> of 16 with exponential backoff retry, and store chunks with full metadata.
> Print a per-collection summary on completion.
> CLI: `python ingest.py data/cv.docx`

---

### Step 4 — Query pipeline (`query.py`)

**What it does:**
1. Accepts a question and experiment parameters via CLI
2. Embeds the question using `mistral-embed`
3. Queries the specified collection with the specified top_k
4. Assembles the prompt (flat or labelled context format)
5. Calls Mistral for generation
6. Prints: retrieved chunks with similarity scores, full prompt, answer,
   token usage
7. Optionally saves result to `outputs/` as JSON

**CLI interface:**
```powershell
python query.py "What did he do at Microsoft?" `
  --collection cv_role_cosine `
  --top-k 3 `
  --model mistral-small-latest `
  --format labelled `
  --save
```

**Output format:**
```
QUERY: What did he do at Microsoft?
COLLECTION: cv_role_cosine | top_k=3 | model=mistral-small

RETRIEVED CHUNKS:
  [1] Microsoft | Senior PM | sim=0.847
      "Initiated cross-device graph offering redesign..."
  [2] Appnexus | Director | sim=0.612
      "Led and scaled a 15-person solutions consulting..."

PROMPT TOKENS: 412 | COMPLETION TOKENS: 89 | TOTAL: 501

ANSWER:
At Microsoft, he initiated a cross-device graph offering redesign...
```

**Claude Code prompt:**
> Build query.py according to the spec. All parameters should be CLI
> arguments with sensible defaults. Print retrieved chunks with similarity
> scores before the answer so retrieval quality is visible. Save to
> outputs/{timestamp}_{collection}_{top_k}.json when --save is passed.
> CLI as specified above.

---

### Step 5 — Stress test runner (`stress_test.py`)

Runs all experiment combinations systematically and saves comparative results.

**Experiment combinations to run:**
```python
collections = [
    "cv_role_cosine", "cv_role_l2",
    "cv_bullet_cosine", "cv_bullet_l2"
]
top_k_by_collection = {
    "cv_role_*":   [1, 3],
    "cv_bullet_*": [1, 5]
}
models = ["mistral-small-latest", "mistral-large-latest"]
formats = ["flat", "labelled"]
```

**Test questions (cover all failure modes):**
```python
questions = [
    # Clearly answerable
    {"q": "What was his role at Utiq?", "in_doc": True},
    {"q": "What languages does he speak?", "in_doc": True},
    {"q": "What did he achieve at Microsoft?", "in_doc": True},
    # Multi-section
    {"q": "Summarise his experience in solutions consulting.", "in_doc": True},
    # Specific metric
    {"q": "What revenue growth did he deliver at Appnexus?", "in_doc": True},
    # Hallucination tests
    {"q": "What is his current salary?", "in_doc": False},
    {"q": "Has he worked in healthcare?", "in_doc": False},
]
```

**Output:** `outputs/stress_test_{timestamp}.json` + printed comparison table

**Claude Code prompt:**
> Build stress_test.py that iterates all experiment combinations defined
> in the spec, runs each question against each combination, records
> similarity scores, token usage, answer text, and whether hallucination
> tests were correctly refused. Save full results to JSON and print a
> summary comparison table at the end.

---

## README structure (write last)

1. What this is and why it was built
2. Architecture diagram (text-based)
3. Setup instructions (Docker + bare Python options)
4. How to run each script with example commands
5. Architectural decisions (link to this doc or inline)
6. Stress test findings — fill in after running experiments
7. What I can say in an interview (personalise after findings)
8. Production upgrade path

---

## Open questions (parking lot)

- Mistral Libraries API — evaluate after raw pipeline is complete. Compare
  managed vs. DIY retrieval quality on the same questions.
- Automatic config acceptance (remove human approval step) — out of scope
  for Week 1, design decision logged here.
- Multi-document ingestion — Week 1 is single document only.
- Hybrid retrieval (semantic + BM25 keyword) — Week 2+ topic.

---
---

# Part 2 — Phase 2 Spec

Continuation of Phase 1 (see Part 1 above for full context and architecture
decisions). This part was the single source of truth for Phase 2
implementation.

Work in two modes:

- **Architecture conversation** — decisions and reasoning (captured here)
- **Claude Code** — implementation only, guided by the steps below

---

## Problem statement (Phase 2)

Phase 1 left one documented gap: the profiler generalises, the chunker doesn't.

`analyse.py` correctly described two additional CVs and diagnosed its own
downstream failures. `chunker.py` ignored the config it had discovered —
its decode rules were hardcoded to the first CV's conventions. On the other
two CVs it produced whole-document mega-chunks with no error. Silent wrong
output is the worst failure mode in a data pipeline.

Phase 2 closes that gap and extends the pipeline to a second document format.

---

## What we're building (Phase 2)

Three deliverables, in strict priority order:

**1. Config-driven chunker** — `chunker.py` reads decode rules from
`config.json` rather than hardcoding them. The profiler was already doing
the hard work. This closes the loop.

**2. Cross-document validation** — run the full pipeline on three
structurally different CVs and produce a comparison table. This is the
portfolio artifact, not the code.

**3. PDF loader (stretch)** — `pdf_parser.py` maps a PDF to the same
paragraph model that `chunker.py` already expects. Most enterprise
documents are PDFs. Even a basic implementation is a meaningful signal.

---

## Why this order

Config-driven chunker first because it's the thing Phase 1 promised and
didn't deliver. "The chunker doesn't, yet" is in the README — shipping
Phase 2 without fixing it would mean the portfolio claim is still false.

Cross-document validation second because it's the proof. Code without
evidence is just code. The comparison table turns the fix into a
demonstrable result that a recruiter can read in 10 seconds and a
technical interviewer can probe.

PDF loader third because it's an extension, not a fix. It broadens the
story only after the core story is true.

---

## Architecture decisions (Phase 2)

### Decision 1 — Config-driven decode rules

**Phase 1 state:**
`chunker.py` contained hardcoded rules:

```python
# Hardcoded — only works for cv.docx
if rendered_size == 14:
    role = "company"
elif style_name == "Heading 3":
    role = "job_title"
```

**Phase 2 state:**
`chunker.py` reads the fingerprint rules from `config.json` at runtime:

```python
# Config-driven — works for any document analyse.py has profiled
rules = config["fingerprint_rules"]  # ordered list, checked in sequence
for rule in rules:
    if matches(paragraph, rule["signal"]):
        return rule["role"]
```

**Why the rules must be ordered:**
The Phase 1 finding still applies — Microsoft is both `Heading 3` AND
14pt. Checking size before style classifies it correctly as company.
The order is set by `analyse.py` + human approval and stored in
`config.json`. `chunker.py` trusts that order and does not re-reason it.

**What changes in `config.json`:**

```json
{
  "fingerprint_rules": [
    { "signal": "has_numPr", "role": "bullet" },
    { "signal": "rendered_size==14", "role": "company" },
    { "signal": "style==Heading 3", "role": "job_title" },
    { "signal": "style==Heading 1", "role": "section_header" }
  ]
}
```

The signal syntax must be parseable by `chunker.py`. Define a small,
explicit signal grammar — no eval(), no dynamic execution:

```python
SIGNAL_PARSERS = {
    "has_numPr":           lambda p: has_num_pr(p),
    "rendered_size=={n}":  lambda p, n: rendered_size(p) == float(n),
    "style=={name}":       lambda p, name: p.style.name == name,
    "is_bold":             lambda p: all(r.bold for r in p.runs if r.text),
}
```

**Why no eval():**
eval() executes arbitrary Python. A config file is user-editable.
Even in a personal project, the habit of not eval()-ing config values
is worth building — it's the right answer in every production context.

---

### Decision 2 — Common paragraph model

**The problem PDF loading introduces:**
`chunker.py` currently receives `python-docx` paragraph objects directly.
PDF paragraphs come from a different library with a different object shape.
If `chunker.py` knows about both, it becomes a format-specific parser —
which defeats the purpose of separating concerns.

**The solution: a common paragraph model.**
Every format-specific loader (`.docx`, `.pdf`) converts its output to
a shared intermediate representation before anything else touches it.
`chunker.py` only ever sees this model — it has no knowledge of source format.

```python
# Common paragraph model — a plain dataclass
@dataclass
class Paragraph:
    text: str
    style_name: str        # "Heading 1", "Normal", etc. (or None)
    rendered_size: float   # pt, after overrides (or None)
    is_bold: bool
    has_num_pr: bool       # True if list item
    in_table: bool
    source_format: str     # "docx" | "pdf" — for diagnostics only
```

**Where each component lives:**

```
loaders/
  docx_loader.py    → .docx → list[Paragraph]
  pdf_loader.py     → .pdf  → list[Paragraph]  (stretch)
models/
  paragraph.py      → Paragraph dataclass
chunker.py          → list[Paragraph] + config → list[Chunk]
analyse.py          → list[Paragraph] → fingerprint map → Mistral → config.json
```

**Why this matters going forward:**
Future multi-format work will need a single intermediate representation.
A common paragraph model means adding a new format is adding one loader —
nothing else changes. This is the pluggable architecture the Phase 1 spec
described as out-of-scope. Phase 2 puts the foundation in place without
building the full registry.

---

### Decision 3 — PDF parsing approach

_(Stretch goal — only if config-driven chunker and validation are complete)_

PDF is structurally different from `.docx`. There is no paragraph style
system, no `numPr` element, no inherited formatting. Structure is inferred
from visual properties: font size, font weight, position on page, whitespace.

**Library choice: `pdfplumber`**

- Extracts text with font metadata (size, bold, font name) per character
- Handles multi-column layouts better than `pypdf`
- Returns bounding box coordinates — useful if positional heuristics
  are needed for structure detection
- Install: `pdfplumber>=0.10.0`

**What the PDF loader does:**

1. Extract text blocks with font metadata via `pdfplumber`
2. Group characters into line objects by vertical position
3. Infer paragraph boundaries from line spacing and font changes
4. Map to `Paragraph` dataclass:
   - `style_name`: None (PDFs have no styles — set explicitly)
   - `rendered_size`: from font metadata
   - `is_bold`: from font name (contains "Bold") or weight
   - `has_num_pr`: detect by line prefix ("•", "-", numbers)
   - `in_table`: basic table detection by alignment

**Known limitation:**
Scanned PDFs (image-based) have no extractable text — `pdfplumber` will
return empty strings. Handling scanned PDFs requires OCR (out of scope).
`pdf_loader.py` should detect this case and raise a clear error rather
than returning empty chunks silently.

**Why pdfplumber over alternatives:**

- `pypdf`: text extraction only, no font metadata
- `pdfminer`: lower-level, verbose, more complex to use correctly
- `pymupdf`: fast and feature-rich but GPL licensed — relevant if this
  ever goes into a commercial context

---

## Repository structure changes (Phase 2)

```
rag-pipeline/
├── docs/                        ← NEW — all documentation in one place
│   ├── SPEC.md                  ← Phase 1 spec (existing, moved)
│   ├── SPEC_PHASE2.md           ← this document
│   └── LEARNING_NOTES.md        ← master learning notes (existing, moved)
├── loaders/                     ← NEW — format-specific parsers
│   ├── __init__.py
│   ├── docx_loader.py           ← refactored from docx_parser.py
│   └── pdf_loader.py            ← NEW (stretch)
├── models/                      ← NEW — shared data models
│   ├── __init__.py
│   └── paragraph.py             ← Paragraph dataclass
├── analyse.py                   ← updated: uses loader, common model
├── chunker.py                   ← updated: config-driven decode
├── review_chunks.py             ← minor update: show config rules used
├── ingest.py                    ← minor update: accepts format flag
├── query.py                     ← unchanged
├── stress_test.py               ← unchanged
├── outputs/
│   └── phase2_validation/       ← NEW — cross-document comparison results
└── data/
    ├── sample_cv.docx           ← existing fake CV
    ├── cv2.docx                 ← second test CV (git-ignored if real)
    └── cv3.docx                 ← third test CV (git-ignored if real)
```

> **Note (post-consolidation):** the `docs/` table above reflects the
> Phase-2-era layout. The current docs layout is just two files:
> `SPEC_RAG_PIPELINE.md` (this file) and `LEARNING_NOTES_RAG_PIPELINE.md`.

**Documentation separation rationale:**

- `docs/` contains all reasoning and learning artifacts
- Root contains only runnable code and config
- A reader of the repo can go straight to `docs/` for the thinking
  or straight to the scripts for the implementation
- README stays at root — it's the entry point, not a deep-dive doc

**.gitignore additions:**

```
# Real test CVs — git-ignored
data/cv2.docx
data/cv3.docx
data/*.pdf

# Phase 2 validation outputs may contain real CV text
outputs/phase2_validation/
```

---

## Implementation steps for Claude Code (Phase 2)

Work through these in order. Do not move to the next step until the
current one produces correct output verified against known ground truth.

---

### Step 1 — Common paragraph model

**What it does:**
Creates the `Paragraph` dataclass that all loaders output and all
downstream components consume.

**Claude Code prompt:**

> Create `models/paragraph.py` with a `Paragraph` dataclass containing
> fields: text (str), style_name (str | None), rendered_size (float | None),
> is_bold (bool), has_num_pr (bool), in_table (bool), source_format (str).
> Add a `__repr__` that shows style_name, rendered_size, and the first
> 60 chars of text — useful for debugging. Add `models/__init__.py`
> exporting Paragraph. No logic beyond the dataclass.

---

### Step 2 — Refactor docx_loader

**What it does:**
Moves the existing `.docx` parsing logic into `loaders/docx_loader.py`
and updates it to output `list[Paragraph]` using the common model.
No behaviour change — same parsing logic, different output type.

**Claude Code prompt:**

> Refactor `docx_parser.py` into `loaders/docx_loader.py`. The public
> interface is a single function: `load_docx(path: str) -> list[Paragraph]`.
> Internally it does exactly what docx_parser.py does today — walk the
> table, deduplicate gridSpan cells, compute rendered font size via the
> style inheritance chain, detect numPr — but outputs Paragraph objects
> instead of raw dicts. Add `loaders/__init__.py`. Update `analyse.py`,
> `chunker.py`, and `review_chunks.py` to import from the new location.
> Run the full pipeline on sample_cv.docx and confirm chunk count matches
> Phase 1 output before proceeding.

---

### Step 3 — Config-driven chunker

**What it does:**
Replaces hardcoded decode rules in `chunker.py` with a rule engine that
reads `fingerprint_rules` from `config.json`.

**Signal grammar** (implement exactly this — no eval()):

```python
# Supported signal patterns:
"has_numPr"              # boolean field check
"rendered_size=={n}"     # float comparison
"style=={name}"          # string equality
"is_bold"                # boolean field check
```

**Claude Code prompt:**

> Update `chunker.py` to read fingerprint_rules from config.json and
> apply them in order using the signal grammar defined in the spec.
> Implement a `parse_signal(signal_str, paragraph) -> bool` function
> using explicit string matching — no eval(), no exec(). The decode
> loop applies rules in config order and returns the first match.
> If no rule matches, classify as "body_text".
>
> Verification: run on sample_cv.docx (Phase 1 CV) and confirm output
> is identical to Phase 1. Then update config.json for cv2.docx,
> run on cv2.docx, confirm chunks are sensible. Document the config
> changes needed per CV.

---

### Step 4 — Cross-document validation

**What it does:**
Runs the full pipeline (analyse → review → ingest → query) on all three
CVs and records results in a structured comparison. Produces the
portfolio artifact.

**Test questions (same across all three CVs — use generic phrasing):**

```python
questions = [
    "What was his most recent role?",
    "What companies has he worked for?",
    "What are his core skills?",
    "Has he managed teams?",              # tests synthesis
    "What is his current salary?",        # hallucination test
]
```

**Comparison table to produce** (`outputs/phase2_validation/comparison.md`):

```markdown
| CV             | Structure           | Phase 1 chunks      | Phase 2 chunks | Notes              |
| -------------- | ------------------- | ------------------- | -------------- | ------------------ |
| sample_cv.docx | Table, mixed styles | 11 role / 25 bullet | 11 / 25        | Baseline unchanged |
| cv2.docx       | ...                 | 3 mega-chunks       | ...            | ...                |
| cv3.docx       | ...                 | 4 mega-chunks       | ...            | ...                |
```

Also record per CV: hallucination refusal rate, top retrieval similarity
for answerable questions, any chunks that were too large or too small.

**Claude Code prompt:**

> Build a `validate.py` script that runs all three CVs through the
> full pipeline using their respective config.json files (stored as
> config_cv1.json, config_cv2.json, config_cv3.json) and records
> chunk counts, query results, similarity scores, and hallucination
> refusal rate for each. Output a markdown comparison table to
> `outputs/phase2_validation/comparison.md` and a JSON file with
> full results. The script should run without interaction — configs
> are pre-approved, validation is automated.

---

### Step 5 — PDF loader (stretch)

**Only start this if Steps 1–4 are complete and verified.**

**What it does:**
Implements `loaders/pdf_loader.py` that maps a PDF to `list[Paragraph]`
using `pdfplumber`. The rest of the pipeline — `analyse.py`, `chunker.py`,
`ingest.py` — is unchanged.

**Claude Code prompt:**

> Add `pdfplumber>=0.10.0` to requirements.txt. Implement
> `loaders/pdf_loader.py` with a single public function:
> `load_pdf(path: str) -> list[Paragraph]`. Use pdfplumber to extract
> text blocks with font metadata. Group into paragraphs by vertical
> position (lines within 2pt of each other = same paragraph). Map to
> Paragraph dataclass: rendered_size from font size, is_bold from font
> name containing "Bold", has_num_pr from line prefix (•, -, digit+dot).
> Detect scanned PDFs (all text empty) and raise ValueError with a clear
> message. Test on one PDF CV. Confirm analyse.py runs on it without
> modification.

---

## README updates (after Phase 2 complete)

Add a **Cross-document validation** section after Stress Test Findings:

```markdown
## Cross-document validation (Phase 2)

| CV             | Structure                   | Phase 1 result          | Phase 2 result    |
| -------------- | --------------------------- | ----------------------- | ----------------- |
| sample_cv.docx | Table, mixed styles         | ✅ 11 clean role chunks | ✅ unchanged      |
| CV #2          | Table, Title style          | ❌ 3 mega-chunks        | ✅ N clean chunks |
| CV #3          | Plain paragraphs, bold+size | ❌ 4 mega-chunks        | ✅ N clean chunks |

Config-driven chunking: the profiler discovers structure, the human approves
the decode rules, the chunker executes them. The same code works on all three
documents — only config.json changes.
```

Update **Production upgrade path** table — config-driven chunker row moves
from "Production" to "This project" ✅.

Update **Reflections** last line from:

> "The tuning earns its keep at scale — which is exactly where this is going next."

To something that reflects Phase 2 is done.

---

## Learning notes to capture (Phase 2)

These are the questions Phase 2 will answer that are worth documenting in
`LEARNING_NOTES_RAG_PIPELINE.md`:

- Does the signal grammar approach (explicit string matching vs eval) hold
  up across real configs? Any signals that didn't fit the grammar?
- What did pdfplumber's font metadata actually look like on a real CV PDF?
  Did bold detection by font name work reliably?
- How different were the config.json files across the three CVs?
  What does that tell us about document class variability?
- Did the common paragraph model create any friction, or did it simplify
  things as expected?
- What would the pluggable strategy registry actually look like now that
  the config-driven decode is built? Is the shape clearer?

---

## Definition of done (Phase 2)

Phase 2 is complete when:

- [x] `chunker.py` is config-driven — no hardcoded decode rules
- [x] Pipeline runs correctly on all three CVs with per-CV config files
- [x] `outputs/phase2_validation/comparison.md` exists with full results
- [x] README updated with comparison table and corrected upgrade path
- [x] `docs/` directory created, spec and learning notes moved there
- [x] PDF loader implemented and tested on one PDF (stretch)
- [x] Learning notes updated with Phase 2 findings
- [x] All changes committed with clear commit messages per step
